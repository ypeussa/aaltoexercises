using UnityEngine;
using System.Collections;

public class FunctionExercise1 : MonoBehaviour {
    // Write a function that takes one integer as an argument
    // and prints out its value times two. (It doesn't return any value.)

    // Call your function from the Start() event to test it.
}
