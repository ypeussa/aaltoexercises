using UnityEngine;
using System.Collections;

public class CodingExercise1 : MonoBehaviour {
	// Write a script that prints out at start "Larger" if the public float variable Number1 is larger than the public float variable Number 2.
	// If Number1 is smaller than Number2, have the script print "Smaller". If it's neither, have it print "Equal".
}